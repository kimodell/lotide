//create a function that returns the frist element of an array
const head = function(arr) {
  //for (let i = 0; i < arr.length; i++) {
    //for loop not needed, you can just return the first element, like so!
  return arr[0];
  //}
};

module.exports = head;
